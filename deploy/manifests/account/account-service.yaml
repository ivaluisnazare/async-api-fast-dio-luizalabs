# service-account.yaml
apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: banking-operations
spec:
  selector:
    app: account-service
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP