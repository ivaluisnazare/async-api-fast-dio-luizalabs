# user-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: banking-operations
spec:
  selector:
    app: user
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
  type: ClusterIP