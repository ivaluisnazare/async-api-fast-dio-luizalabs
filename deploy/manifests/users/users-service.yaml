# users-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: banking-operations
  labels:
    app: users-service
spec:
  selector:
    app: users-service
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
  type: ClusterIP